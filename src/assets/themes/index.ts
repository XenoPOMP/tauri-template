import { defaultTheme } from './defaultTheme';
import { lightTheme } from './lightTheme';
import { themesMap } from './themes-map';

type CustomTheme = typeof defaultTheme;

export { defaultTheme, type CustomTheme, lightTheme, themesMap };
